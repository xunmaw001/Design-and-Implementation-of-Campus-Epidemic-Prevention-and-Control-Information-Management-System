<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.DakaDao">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        a.id as id
        ,a.yonghu_id as yonghuId
        ,a.daka_name as dakaName
        ,a.daka_file as dakaFile
        ,a.daka_jiuzhen as dakaJiuzhen
        ,a.daka_types as dakaTypes
        ,a.daka_wendu as dakaWendu
        ,a.daka_text as dakaText
        ,a.daka_delete as dakaDelete
        ,a.insert_time as insertTime
        ,a.create_time as createTime
    </sql>
    <select id="selectListView" parameterType="map" resultType="com.entity.view.DakaView" >
        SELECT
        <include refid="Base_Column_List" />
        <!-- 级联表的字段 -->
        ,yonghu.yonghu_uuid_number as yonghuUuidNumber
        ,yonghu.yonghu_name as yonghuName
        ,yonghu.yonghu_phone as yonghuPhone
        ,yonghu.yonghu_id_number as yonghuIdNumber
        ,yonghu.yonghu_photo as yonghuPhoto
        ,yonghu.yonghu_types as yonghuTypes
        ,yonghu.yonghu_guankong_types as yonghuGuankongTypes
        ,yonghu.yonghu_email as yonghuEmail

        FROM daka  a
        left JOIN yonghu yonghu ON a.yonghu_id = yonghu.id

        <where>
            <if test="params.ids != null">
                and a.id in
                <foreach item="item" index="index" collection="params.ids" open="(" separator="," close=")">
                #{item}
                </foreach>
            </if>
            <if test="params.yonghuId != null and params.yonghuId != ''">
                and (
                    a.yonghu_id = #{params.yonghuId}
                )
            </if>
            <if test=" params.dakaName != '' and params.dakaName != null and params.dakaName != 'null' ">
                and a.daka_name like CONCAT('%',#{params.dakaName},'%')
            </if>
            <if test=" params.dakaJiuzhen != '' and params.dakaJiuzhen != null and params.dakaJiuzhen != 'null' ">
                and a.daka_jiuzhen like CONCAT('%',#{params.dakaJiuzhen},'%')
            </if>
            <if test="params.dakaTypes != null and params.dakaTypes != ''">
                and a.daka_types = #{params.dakaTypes}
            </if>
            <if test="params.dakaWenduStart != null ">
                <![CDATA[  and a.daka_wendu >= #{params.dakaWenduStart}   ]]>
            </if>
            <if test="params.dakaWenduEnd != null ">
                <![CDATA[  and a.daka_wendu <= #{params.dakaWenduEnd}   ]]>
            </if>
            <if test="params.dakaWendu != null and params.dakaWendu != ''">
                and a.daka_wendu = #{params.dakaWendu}
            </if>
            <if test=" params.dakaText != '' and params.dakaText != null and params.dakaText != 'null' ">
                and a.daka_text like CONCAT('%',#{params.dakaText},'%')
            </if>
            <if test="params.dakaDeleteStart != null and params.dakaDeleteStart != ''">
                <![CDATA[  and a.daka_delete >= #{params.dakaDeleteStart}   ]]>
            </if>
            <if test="params.dakaDeleteEnd != null and params.dakaDeleteEnd != ''">
                <![CDATA[  and a.daka_delete <= #{params.dakaDeleteEnd}   ]]>
            </if>
             <if test="params.dakaDelete != null and params.dakaDelete != ''">
                and a.daka_delete = #{params.dakaDelete}
             </if>
            <if test=" params.insertTimeStart != '' and params.insertTimeStart != null ">
                <![CDATA[  and UNIX_TIMESTAMP(a.insert_time) >= UNIX_TIMESTAMP(#{params.insertTimeStart}) ]]>
            </if>
            <if test=" params.insertTimeEnd != '' and params.insertTimeEnd != null ">
                <![CDATA[  and UNIX_TIMESTAMP(a.insert_time) <= UNIX_TIMESTAMP(#{params.insertTimeEnd}) ]]>
            </if>
            <if test="params.insertTime != null and params.insertTime != ''">
                and a.insert_time = #{params.insertTime}
            </if>

                <!-- 判断用户的id不为空 -->
            <if test=" params.yonghuIdNotNull != '' and params.yonghuIdNotNull != null and params.yonghuIdNotNull != 'null' ">
                and a.yonghu_id IS NOT NULL
            </if>
            <if test=" params.yonghuUuidNumber != '' and params.yonghuUuidNumber != null and params.yonghuUuidNumber != 'null' ">
                and yonghu.yonghu_uuid_number like CONCAT('%',#{params.yonghuUuidNumber},'%')
            </if>
            <if test=" params.yonghuName != '' and params.yonghuName != null and params.yonghuName != 'null' ">
                and yonghu.yonghu_name like CONCAT('%',#{params.yonghuName},'%')
            </if>
            <if test=" params.yonghuPhone != '' and params.yonghuPhone != null and params.yonghuPhone != 'null' ">
                and yonghu.yonghu_phone like CONCAT('%',#{params.yonghuPhone},'%')
            </if>
            <if test=" params.yonghuIdNumber != '' and params.yonghuIdNumber != null and params.yonghuIdNumber != 'null' ">
                and yonghu.yonghu_id_number like CONCAT('%',#{params.yonghuIdNumber},'%')
            </if>
            <if test="params.yonghuTypes != null  and params.yonghuTypes != ''">
                and yonghu.yonghu_types = #{params.yonghuTypes}
            </if>

            <if test="params.yonghuGuankongTypes != null  and params.yonghuGuankongTypes != ''">
                and yonghu.yonghu_guankong_types = #{params.yonghuGuankongTypes}
            </if>

            <if test=" params.yonghuEmail != '' and params.yonghuEmail != null and params.yonghuEmail != 'null' ">
                and yonghu.yonghu_email like CONCAT('%',#{params.yonghuEmail},'%')
            </if>
        </where>
        order by a.${params.sort} ${params.order}
    </select>

</mapper>